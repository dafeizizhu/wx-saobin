import React, { Component } from 'react'
import { connect } from 'react-redux'

class OwnACarSubmitRs extends Component {
  render() {
    return <div>own a car rs</div>
  }
}

export default connect(state => state)(OwnACarSubmitRs)
